---
name: run-app
description: Start the Encore application locally with automatic infrastructure provisioning.
---

# Run Encore App

1. Ensure Docker is running (required for local databases and caches).
2. Run `encore run` in the project root.
3. The local development dashboard is available at http://localhost:9400.
4. API endpoints are available at http://localhost:4000.
